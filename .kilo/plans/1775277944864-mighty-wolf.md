# Plano Unificado de Refatoracao — FocoZen Pro

## Visao Geral

A migracao para React esta **completa em todos os 6 dominios**. O app funciona corretamente. O que resta sao: limpeza de codigo morto, remocao de duplicacoes residuais, e hardening com testes.

---

## O Que Ja Foi Feito (DONE)

### Fase 1 — Cleanup
- ✅ `renderer.js` monolitico removido (dividido em 27 modulos em `src/renderer/`)
- ✅ `src/legacy-renderer/` removido
- ✅ Arquivos mortos deletados: `App.jsx`, `SettingsReactSandbox.jsx`, `runtime.js`, `debug-log.txt`, `implementation_plan.md`, etc.
- ✅ `--max-old-space-size` aumentado de 256 para 512
- ✅ Duplicacoes de dados removidas — todos os constantes agora vem de `window.FocoZenConstants` / `window.FocoZenStorage`
- ✅ `escapeHtml` usado em todos os `innerHTML` com dados do usuario
- ✅ Username renderizado via `textContent` (XSS-safe)

### Fase 2 — Migracao React
- ✅ Home → `HomeReactView.jsx` + `home-runtime.js`
- ✅ Timer → `TimerReactPanel.jsx` + `timer-runtime.js`
- ✅ Tarefas → `TasksReactSidebar.jsx` + `tasks-runtime.js`
- ✅ Metas → `GoalsReactView.jsx` + `goals-runtime.js`
- ✅ Estatisticas → `StatsReactView.jsx` + `stats-runtime.js`
- ✅ Assistente → `AssistantReactDock.jsx` + `assistant-runtime.js`
- ✅ Settings → `SettingsReactView.jsx`
- ✅ Shell React → `AppShell.jsx` com navegacao entre 4 views

### Fase 3 — Arquitetura
- ✅ `src/core/` — 6 modulos de logica pura (`constants`, `utils`, `history`, `goals`, `timer`, `assistant`)
- ✅ `src/services/` — 5 bridges (`storage`, `audio`, `pip`, `updates`, `task-session`)
- ✅ `src/react/` — 7 componentes + 6 contracts + shell + entry
- ✅ Runtime contracts via `window.FocoZen*Runtime` — padrao consistente
- ✅ Testes existentes: 3 arquivos em `tests/contracts/` + `vitest.config.mjs`

---

## O Que Falta (PENDING)

### Prioridade 1 — Codigo Morto nos Modulos Renderer (~150 linhas)

#### 1.1 `dashboard.js` — remover 4 funcoes nao usadas
- `filterHistoryForPeriod` — definida mas nunca chamada (o core `historyCore.filterHistoryByPeriod` ja faz isso)
- `getGoalSummariesData` — definida mas nunca chamada
- `getDailyGoalOutcomeData` — definida mas nunca chamada
- `populateGoalForm` — definida mas nunca chamada (substituida por `openGoalEditModal`)

#### 1.2 `chart-rendering.js` — remover funcao duplicada
- `filterHistoryByPeriod` — duplicata da versao em `historyCore`, nao usada por ninguem (o React usa `historyCore.filterHistoryByPeriod` diretamente)

#### 1.3 `test-utils.js` — remover arquivo de debug
- `window.testChangelog` — funcao de desenvolvimento, nao necessaria em producao
- Referenciada apenas em comentario em `assistant-advanced.js`

#### 1.4 `timeline.js` — modulo inteiro morto
- `renderTimeline` — referencia `focusTimeline` que nao existe no `index.html`
- `formatMinsToHours` — duplicata de `formatMinutesToHours` em `dashboard.js`
- Nenhum elemento HTML `#focusTimeline` ou `#timelineTooltip` ou `#timelineSummary` existe no HTML

#### 1.5 `sound-carousel.js` — modulo inteiro morto
- `buildSoundCarousel` / `toggleSoundCarousel` — referenciam `#miniSoundCarousel`, `#miniSoundSwitchBtn`, `#settingsSoundCarousel`, `#settingsSoundSwitchBtn` — nenhum existe no `index.html`
- O sound carousel foi substituido pelo grid de sons no React Home

#### 1.6 `update-system.js` — verificar se ainda necessario
- Funcoes: `setupUpdateListeners`, `showUpdateNotification`, `formatReleaseNotes`, `checkForChangelog`, `showChangelogModal`, `formatFullChangelog`, `handleUpdateCheckResult`, `initSobreSection`
- Chamado por `sound-carousel.js:43` (`setupUpdateListeners()`) — que tambem sera removido
- **Verificar**: `initSobreSection` e `checkForChangelog` sao chamados no bootstrap? Se sim, mover para modulo apropriado. Se nao, remover.

### Prioridade 2 — Consolidar Modulos Redundantes

#### 2.1 `timeline.js` + `sound-carousel.js` + `update-system.js`
- `timeline.js` e `sound-carousel.js` sao 100% mortos → deletar
- `update-system.js` contem logica valida de updates → manter, mas remover dependencia do `sound-carousel.js`

#### 2.2 `formatMinsToHours` duplicada
- Existe em `chart-rendering.js:15` E `dashboard.js:1` → manter apenas a de `dashboard.js` (que delega para `historyCore`)

### Prioridade 3 — Cache-Busting Temporario

#### 3.1 Remover `?v=2` de todos os scripts em `index.html`
- Solucao temporaria adicionada durante a migracao
- Substituir por hash-based caching via Vite ou remover quando stable

### Prioridade 4 — Expandir Testes Automatizados

#### 4.1 Testes de contratos faltantes
- `tests/contracts/stats-runtime.test.js` — nao existe
- `tests/contracts/goals-runtime.test.js` — nao existe
- `tests/contracts/assistant-runtime.test.js` — nao existe

#### 4.2 Testes de core modules
- `tests/core/timer.test.js` — `getTimerProgress`, `formatTimerLabel`, `getTaskSessionDurationSeconds`
- `tests/core/history.test.js` — `formatMinutesToHours`, `getFocusWindowSummary`, `getFocusStreakSummary`
- `tests/core/goals.test.js` — `getGoalOverview`, `getGoalMomentumState`, `getSortedGoalCategories`
- `tests/core/assistant.test.js` — `buildReply`, intent detection

#### 4.3 Testes de componentes React (RTL)
- `tests/react/TimerReactPanel.test.jsx` — renderiza, clica play/reset
- `tests/react/TasksReactSidebar.test.jsx` — renderiza tasks, toggle complete
- `tests/react/HomeReactView.test.jsx` — sound grid, volume, category

### Prioridade 5 — Legibilidade do `main.js`

#### 5.1 Reformatar linhas longas
- `if (pipWindow && !pipWindow.isVisible()) { pipWindow.show(); pipWindow.focus(); }` → multi-line
- Comentarios removidos (bom), mas formacao de blocos pode melhorar

---

## Ordem de Execucao Recomendada

1. **Prioridade 1** (30 min) — Remover codigo morto dos modulos renderer
2. **Prioridade 2** (15 min) — Deletar `timeline.js`, `sound-carousel.js`, consolidar `update-system.js`
3. **Prioridade 3** (10 min) — Remover cache-busting `?v=2`
4. **Prioridade 4** (2-3h) — Expandir suite de testes
5. **Prioridade 5** (15 min) — Reformatar `main.js`

**Total estimado:** 3-4 horas de trabalho.

---

## Metricas Atuais

| Metrica | Valor |
|---|---|
| `src/renderer/` arquivos | 27 |
| `src/renderer/` linhas totais | ~5,551 |
| `src/react/` arquivos | 16 |
| `src/core/` arquivos | 6 |
| `src/services/` arquivos | 5 |
| Testes existentes | 3 arquivos |
| Modulos mortos identificados | `timeline.js`, `sound-carousel.js`, `test-utils.js` |
| Funcoes mortas identificadas | 4 em `dashboard.js`, 1 em `chart-rendering.js` |

---

## Riscos e Observacoes

1. **`update-system.js`** — contem logica valida de updates. Verificar que `setupUpdateListeners()` e chamado em outro lugar antes de remover a dependencia de `sound-carousel.js`.
2. **Ordem de carregamento** — os 27 scripts em `index.html` dependem de ordem correta (state-and-constants primeiro, depois os demais). Nao reordenar sem testar.
3. **Cache-busting `?v=2`** — remover so quando tudo estiver stable.
4. **`timeline.js` e `sound-carousel.js`** — confirmar que nenhum elemento HTML referenciado existe antes de deletar.
