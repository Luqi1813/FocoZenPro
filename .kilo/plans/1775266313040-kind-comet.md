# Status Atual e Próximos Passos — FocoZen Pro

## Onde Paramos

Estávamos no meio da **Fase 1 — Limpeza Imediata**. A maioria dos arquivos mortos foi deletada, mas as mudanças estão **unstaged** (não commitadas). O chat travou antes de finalizar.

## Status Detalhado

### Fase 1 — Limpeza Imediata (90% completa)

| Arquivo | Status |
|---|---|
| `src/react/App.jsx` | ✅ Deletado (unstaged) |
| `src/react/components/SettingsReactSandbox.jsx` | ✅ Deletado (unstaged) |
| `src/react/contracts/runtime.js` | ✅ Deletado (unstaged) |
| `debug-log.txt` | ✅ Deletado (unstaged) |
| `src/legacy-renderer/assistant.js` | ✅ Deletado (unstaged) |
| `src/legacy-renderer/goals-stats.js` | ✅ Deletado (unstaged) |
| `implementation_plan.md` | ✅ Não encontrado (já removido) |
| `implemention plan.md` | ✅ Não encontrado (já removido) |
| `nul` | ✅ Não encontrado (já removido) |

**Mudanças adicionais detectadas (não planejadas originalmente):**
- `index.html` — modificado (391 linhas alteradas, provavelmente migração React)
- `main.js` — modificado (+20 linhas)
- `renderer.js` — modificado (-360 linhas, código extraído)
- `pip.html` — modificado
- `styles.css` — modificado
- `package.json` / `package-lock.json` — modificados (novas deps)
- `src/react/components/TimerReactPanel.jsx` — modificado
- `src/react/main.jsx` — modificado

**Novos arquivos criados (untracked):**
- `src/react/components/SettingsReactView.jsx`
- `src/react/shell/` (diretório com AppShell e outros)
- `tests/` (diretório de testes)
- `vitest.config.mjs`
- `.kilo/` (configuração do Kilo)

### Fase 2 — Performance e Segurança (NÃO iniciada)

| Tarefa | Status |
|---|---|
| 2.1 Remover duplicações de dados em renderer.js | ❌ Não iniciado |
| 2.2 Aumentar `--max-old-space-size` de 256 para 512 | ❌ Não iniciado |
| 2.3 Sanitização de inputs (escapeHtml) | ❌ Não iniciado |

### Fase 3 — Modularização do renderer.js (NÃO iniciada)

| Tarefa | Status |
|---|---|
| Criar `src/renderer/state.js` | ❌ Não iniciado |
| Criar módulos por domínio | ❌ Não iniciado |
| Migrar para ES modules | ❌ Não iniciado |

### Fase 4 — Hardening (NÃO iniciada)

| Tarefa | Status |
|---|---|
| Testes de core modules | ❌ Não iniciado |
| Testes RTL de componentes React | ❌ Não iniciado |
| Smoke matrix completa | ❌ Não iniciado |

---

## Próximos Passos Imediatos

### Passo 1: Finalizar Fase 1 (5 min)

1. **Commit das mudanças pendentes** — todas as deleções e modificações estão unstaged
2. **Verificar que app funciona** — `npm run react:build` e smoke test rápido
3. **Rodar testes existentes** — `npm run test`

### Passo 2: Iniciar Fase 2.1 — Remover Duplicações (~30 min)

O `renderer.js` ainda tem ~7150 linhas com dados duplicados de `constants.js`:
- `soundsConfig` → usar `window.FocoZenConstants.soundsConfig`
- `storageKeys` → usar `window.FocoZenStorage.storageKeys`
- `quotes`, `successQuotes` → usar `window.FocoZenConstants.quotes`
- `motivationalRestartMessages` → usar `window.FocoZenConstants.motivationalRestartMessages`
- Constantes de timer → usar `window.FocoZenConstants`
- `defaultCategories`, `soundCategories`, `soundThemes` → usar `window.FocoZenConstants`

### Passo 3: Fase 2.2 — Aumentar RAM (5 min)

`main.js:34`: mudar `--max-old-space-size=256` para `512`

### Passo 4: Fase 2.3 — Sanitização (30 min)

Adicionar `escapeHtml()` antes de `innerHTML` com:
- Task names em `renderTasksList()` e `renderTasksSidebar()`
- Username em `updateSidebarProfile()`
- Assistant messages

---

## Riscos e Atenções

1. **Muitas mudanças unstaged** — há modificações em `index.html`, `renderer.js`, `main.js` que não foram parte do plano original. Precisa verificar se são migrações React legítimas ou algo inesperado.
2. **Branch `codex/refactor-legacy-tasks`** — 5 commits ahead do origin. Push pendente.
3. **renderer.js ainda com 7150 linhas** — a Fase 3 será a mais complexa.

## Ordem Recomendada

1. ✅ Commit das mudanças pendentes (Fase 1 finalizada)
2. ⏭️ Fase 2.1 — Remover duplicações
3. ⏭️ Fase 2.2 — Aumentar RAM
4. ⏭️ Fase 2.3 — Sanitização
5. ⏭️ Fase 3 — Modularização (maior esforço)
6. ⏭️ Fase 4 — Hardening

---

## Resumo para o Usuário

**Fase 1 está 90% completa.** Todos os 7 arquivos mortos foram deletados, mas as mudanças nunca foram commitadas porque o chat travou. 

**Próximo passo imediato:** commitar tudo, rodar testes, e partir para a Fase 2.1 (remover duplicações de dados no renderer.js).

---

## Resumo para o Usuário

**Fase 1 está 90% completa.** Todos os 7 arquivos mortos foram deletados, mas as mudanças nunca foram commitadas porque o chat travou. 

**Próximo passo imediato:** commitar tudo, rodar testes, e partir para a Fase 2.1 (remover duplicações de dados no renderer.js).
